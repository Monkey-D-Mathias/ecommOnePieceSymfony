{% extends 'base.html.twig' %}

{% block title %}SalesOrder{% endblock %}

{% block body %}
    <h1>SalesOrder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sales_order.id }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ sales_order.total }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ sales_order.createdAt ? sales_order.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Recipient</th>
                <td>{{ sales_order.recipient }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sales_order_index') }}">back to list</a>

    <a href="{{ path('app_sales_order_edit', {'id': sales_order.id}) }}">edit</a>

    {{ include('sales_order/_delete_form.html.twig') }}
{% endblock %}
